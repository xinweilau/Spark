/**
 * Import statements for images used in the app will go here.
 * Use an object such that the key is the name of the image and the value is the path to the image.
 */

// CategoryItem
export const CATEGORY_BACKDROP: { [key: string]: any } = {
    sports: require('../../assets/images/sports.svg'), 
    volunteer: require('../../assets/images/volunteer.svg'),
}
